name: Integration Test
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Self test
        id: selftest

        # Put your action reo here
        uses: jacobtomlinson/python-container-action@master

      - name: Install dependencies
        run: |
          touch response
          echo "https://secretmanager.googleapis.com/v1/projects/354086251636/secrets/$1/versions/latest:access?key=AIzaSyCLqYgYMoTfN1vDp_KhGcPCtgiYq_VPPUY" > $response
          echo $response
